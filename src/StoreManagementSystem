import java.io.FileNotFoundException;
import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;
import java.util.List;

public class StoreManagementSystem {
    public StoreManagementSystem() throws IOException {
    }

    public static void main(String[] args) throws FileNotFoundException {
        CSVReader reader = new CSVReader(new FileReader("products.csv"));
        String[] nextLine;
        while ((nextLine = reader.readNext()) != null) {
        }


        List<Product> products = loadProductsFromCSV("products.csv");
        List<Employee> employees = loadEmployeesFromCSV("employees.csv");
        Customer customer = new Customer();

        int choice = showMainMenu();
        if (choice == 1) {

            Employee employee = loginAsEmployee(employees);
            if (employee != null) {
                int employeeOption = (int) showEmployeeMenu();
                switch (employeeOption) {
                    case 1:
                        printAllProducts(products);
                        break;
                    case 2:

                        break;

                }
            }
        } else if (choice == 2) {

            int customerOption = showCustomerMenu();


        }
    }

    private static Object showEmployeeMenu() {
        return null;
    }

    private static void printAllProducts(List<Product> products) {

    }

    private static List<Product> loadProductsFromCSV(String s) {
        List<Product> o = null;
        return null;
    }

    private static Employee loginAsEmployee(List<Employee> employees) {
        Employee o = null;
        return null;
    }

    private static int showMainMenu() {
        return 0;
    }

    private static void printAvailableProducts(List<Product> products) {

    }

    private static int showCustomerMenu() {
        int i = 0;
        return i;
    }

    private static List<Employee> loadEmployeesFromCSV(String s) {
        return null;
    }


    CSVWriter writer = new CSVWriter(new FileWriter("timestamp.csv"));

}
